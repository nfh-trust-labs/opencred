<!-- PRs target the `opencred-dev` branch, not `main`. -->

## Summary

<!-- What does this PR do, and why? Link the issue: Closes #NNN -->

## Changes

-

## Test plan

<!-- How was this verified? Commands run, tests added, manual steps. -->

## Checklist

- [ ] PR targets `opencred-dev`
- [ ] Commits follow Conventional Commits and are signed off (`git commit -s`, DCO)
- [ ] Tests added/updated and `pnpm test` passes
- [ ] `pnpm lint` and `pnpm typecheck` pass
- [ ] Docs updated — repo-wide grep found no stale references to changed behavior
- [ ] No backward-compatibility break (API shapes, env vars, output formats, on-disk formats) — or the break is flagged above in bold
- [ ] No issuer private key material touched, logged, or transmitted by any new code path
