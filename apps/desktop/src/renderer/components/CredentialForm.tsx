/**
 * CredentialForm — placeholder for the desktop app.
 *
 * This mirrors the web app's CredentialForm component. The full implementation
 * will be added in a follow-up issue when the desktop credential workflow is
 * built out.
 */
export function CredentialForm() {
  return (
    <div className="rounded-lg border border-gray-200 bg-white p-4">
      <h2 className="text-sm font-medium text-gray-700">Credential Details</h2>
      <p className="mt-1 text-sm text-gray-500">
        The credential form will be available once the desktop credential workflow is implemented
        (see issue #37).
      </p>
    </div>
  );
}
