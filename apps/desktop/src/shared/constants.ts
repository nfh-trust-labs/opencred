/**
 * Shared constants for the desktop application.
 *
 * This module is importable from main, renderer, and test code without
 * pulling in Electron dependencies.
 */

/** Maximum number of rows allowed in a single batch issuance. */
export const BATCH_ROW_LIMIT = 1000;
