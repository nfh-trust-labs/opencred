{
  "name": "@opencred/signing-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "install-host": "node dist/install/install-host.js",
    "uninstall-host": "node dist/install/uninstall-host.js"
  },
  "dependencies": {
    "@opencred/signing": "workspace:*",
    "@opencred/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
