# Sample Data

Demo scripts generate fresh ephemeral P-256 key pairs at runtime using CSPRNG.
No static private or public key files are needed — all cryptographic material
is created ephemerally in memory during demo execution and never persisted.

These keys are for **demo and development purposes ONLY** and must NEVER be
used in production or for real credential issuance.
