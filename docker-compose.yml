# ==============================================================================
# OpenCred — Docker Compose (Production)
# ==============================================================================
# The `server` service (apps/server) will be added in Phase 7.
# It replaces the old apps/api + apps/web services.
#
# Usage (Phase 7+):
#   docker compose up -d            # Start OpenCred server
#   docker compose down              # Stop all services
#   docker compose logs -f server    # Follow server logs
# ==============================================================================

volumes:
  csca-trust-store:
    driver: local

networks:
  opencred-net:
    driver: bridge
