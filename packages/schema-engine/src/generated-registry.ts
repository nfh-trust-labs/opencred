// AUTO-GENERATED PLACEHOLDER — overwritten by scripts/fetch-and-embed-schemas.mjs
// during `pnpm --filter @opencred/schema-engine build`. Committed so the package
// can `tsc` before the build script has been run against opencred-vc-schemas.
import { SchemaRegistry } from "./schema-registry.js";

export function createBuiltInRegistry(): SchemaRegistry {
  return new SchemaRegistry();
}
